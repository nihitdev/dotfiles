# Atuin

Catppuccin Mocha colors for searchable shell history.

Copy `config.toml` to `~/.config/atuin/config.toml` or use `install.sh --only atuin`. Authentication and history sync remain local to each machine.
