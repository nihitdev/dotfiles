# Atuin

Catppuccin Mocha colors for searchable shell history.

Copy `config.toml` to `~/.config/atuin/config.toml`. The Windows installer handles this automatically; authentication and history sync remain local to each machine.
