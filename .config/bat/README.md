# Bat

Bat configuration and Catppuccin themes for syntax-highlighted file viewing.

Copy `config` and `themes/` into the directory reported by `bat --config-dir`, then run `bat cache --build` to rebuild the theme cache.
