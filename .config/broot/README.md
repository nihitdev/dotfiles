# Broot

Broot navigation settings, custom verbs, and a Catppuccin Mocha skin.

Install this directory to `~/.config/broot/`. Keep `conf.hjson`, `verbs.hjson`, and `skins/` together so their references continue to resolve.
