# Broot

Broot navigation settings, custom verbs, and a Catppuccin Mocha skin.

Copy this directory to `%APPDATA%\dystroy\broot\config` on Windows. Keep `conf.hjson`, `verbs.hjson`, and `skins/` together so their references continue to resolve.
