# ExplorerBlurMica

Backdrop and appearance settings for ExplorerBlurMica on Windows.

Back up the application's existing `config.ini`, then copy this version beside the ExplorerBlurMica executable. Its installation directory varies by package manager, so this file is not installed automatically.
