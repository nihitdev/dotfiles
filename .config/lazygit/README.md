# LazyGit

Catppuccin Mocha styling for the LazyGit terminal interface.

Copy `config.yml` to `%LOCALAPPDATA%\lazygit\config.yml` on Windows, or merge its `gui.theme` section into an existing configuration.
