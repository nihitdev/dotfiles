# Neovim

LazyVim-based configuration with pinned plugins, custom options, keymaps, and dashboard styling.

Copy this directory to `~/.config/nvim` or use `install.sh --only nvim`. Start `nvim` afterward to let `lazy.nvim` install the versions recorded in `lazy-lock.json`.
