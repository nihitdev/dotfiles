# Neovim

LazyVim-based configuration with pinned plugins, custom options, keymaps, and dashboard styling.

Copy this directory to `%LOCALAPPDATA%\nvim` on Windows or `~/.config/nvim` on Linux. Start `nvim` afterward to let `lazy.nvim` install the versions recorded in `lazy-lock.json`.
