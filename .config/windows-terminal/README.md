# Windows Terminal

Windows Terminal profiles, colors, font choices, and appearance settings.

Back up the existing `settings.json` before copying this file into the package's `LocalState` directory. Review machine-specific profile identifiers before replacing an established setup.
