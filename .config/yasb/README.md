# YASB

Yet Another Status Bar configuration and Catppuccin-inspired CSS styling for Windows.

Copy `config.yaml` and `styles.css` to `~/.config/yasb`. Install the fonts and widgets referenced by the configuration before launching YASB.
