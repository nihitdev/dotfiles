# Neovim

LazyVim-based Neovim configuration with a transparent Catppuccin Mocha theme,
Copilot and a custom Snacks dashboard.

On Windows, install it at `%LOCALAPPDATA%\nvim`. The repository installer does
this automatically. Neovim bootstraps lazy.nvim and the remaining plugins on
first launch; Git and network access are required.
