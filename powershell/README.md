# PowerShell

PowerShell 7 profile with prompt initialization, cached shell integrations, navigation helpers, and modern CLI aliases.

Copy `profile.ps1` to the path shown by `$PROFILE.CurrentUserAllHosts`. Optional tools are detected at startup, so the profile remains usable when they are absent.
