# Starship

Minimal cross-shell prompt configurations with directory and Git information.

Use `starship.toml` as the general configuration, or `zsh.toml` for the Zsh variant. Copy the chosen file to `~/.config/starship.toml` and initialize Starship in your shell.
